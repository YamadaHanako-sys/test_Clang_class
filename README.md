# test_Clang_class
C言語を用いた授業内成果物の保存用リポジトリ
実行

制作環境
言語: C言語

コンパイラ: gcc (Ubuntu 11.4.0)

開発ツール: VS Code / Terminal



#二分木探索プログラム（C言語）

C言語による二分探索木のスクラッチ実装です。

## 概要
このプロジェクトは、基本的なデータ構造である二分探索木をC言語で実装したものです。
独自のデータ構造定義から、ノードの追加、削除、再帰的な木構造の表示までの一連の機能を備えています。

## 制作背景
ライブラリに頼らず、データ構造の内部動作を深く理解するために制作しました。
特に以下の技術的要素に重点を置いています。
* **ポインタの深い理解**: 構造体ポインタやダブルポインタを用いたノードの繋ぎ替え。
* **メモリ管理**: `calloc` による動的確保と `free` による適切なメモリ解放。
* **アルゴリズムの実装**: 再帰処理を用いた木の走査や、削除時のノードの再配置ロジック。

## 機能
* **データの追加 (`add`)**: 二分探索木のルールに基づき、適切な位置にノードを挿入します。
* **データの削除 (`remove_node`)**: 
  * 子を持たないノード
  * 子を1つ持つノード
  * 子を2つ持つノード（左部分木の最大値を探索して移動）
  それぞれのケースに対応した削除ロジックを実装しています。
* **木構造の表示 (`printtree`)**: 親子関係が視覚的にわかる形式でターミナルに出力します。

## ファイル構成
| ファイル名 | 役割 |
| :--- | :--- |
| `main.c` | プログラムの実行例（エントリーポイント） |
| `bintree.c` / `.h` | 二分探索木の基本ロジックと構造体定義 |
| `mydata.c` / `.h` | 木に格納する具体的なデータ（ID・名前）の操作 |

## 実行方法

コンパイルには `gcc` などのCコンパイラを使用してください。

```bash
# コンパイル
gcc -o bst_demo main.c bintree.c mydata.c

# 実行
./bst_demo


ーーーーーーーーーーーーーーーーーーーーーーー


# 🚀 C-Core Algos & Data Structures Portfolio

C言語によるデータ構造のスクラッチ実装から、実務的な数理アルゴリズムまでを網羅したプロジェクト集です。標準ライブラリに頼らず、メモリ管理やポインタ操作などの低レイヤー処理を意識して実装しています。

## 📌 目次
1. [Binary Search Tree（二分探索木）](#1-binary-search-tree二分探索木)
2. [Queue（キュー）](#2-queueキュー)
3. [Score（成績データ処理・ソート）](#3-score成績データ処理ソート)
4. [Stack（スタック）](#4-stackスタック)
5. [En（円の面積計算）](#5-en円の面積計算)
6. [Graph（二次関数可視化）](#6-graph二次関数可視化)
7. [Manki（元金算出）](#7-manki元金算出)
8. [Nizi（三角形面積計算）](#8-nizi三角形面積計算)
9. [Proot（内部収益率 IRR）](#9-proot内部収益率-irr)
10. [Statistic（統計処理）](#10-statistic統計処理)
11. [Youbi（日数計算）](#11-youbi日数計算)

---

## 1. Binary Search Tree（二分探索木）
### 📝 概要
独自のデータ構造定義から、ノードの追加、削除、再帰的な木構造の表示までの一連の機能を備えた二分探索木の実装です。

### ✨ 特徴
* **ポインタの深い理解**: 構造体ポインタやダブルポインタを用いたノードの繋ぎ替えを実装。
* **メモリ管理**: `calloc` による動的確保と `free` による適切なメモリ解放。
* **アルゴリズム**: 再帰処理を用いた木の走査や、複雑なノード削除ロジック（子を2つ持つケース等）を網羅。

### 📂 ファイル構成
* `bintree.c`: 二分探索木の本体ロジック。
* `bintree.h`: 構造体および関数のプロトタイプ宣言。
* `main.c`: 追加・削除・表示のデモンストレーション。

### 💻 実行方法
```bash
gcc -o bintree main.c bintree.c
./bintree



ーーーーーーーーーーーーーーー

2. Queue（キュー）📝 概要FIFO (First-In, First-Out) の原則に基づくデータ構造です。効率的なメモリ利用のため**リングバッファ（循環配列）**を採用しています。✨ 特徴リングバッファ: データの取り出し時に要素をずらす必要がなく、処理速度 $O(1)$ を実現。状態管理: front と rear のインデックス管理により、配列終端から先頭への循環を制御。📂 ファイル構成queue.c: キュー（リングバッファ）の実装。queue.h: 構造体定義。main.c: キュー操作のテストプログラム。💻 実行方法Bashgcc -o queue main.c queue.c
./queue
